# Contracts Package

This path is reserved for backend-owned shared contracts.

Expected contents in later issues:

- DTO definitions
- OpenAPI-derived artifacts
- shared TypeScript API client shapes
- event payload definitions

Backend schemas remain authoritative. iOS should consume generated artifacts from the same source of truth rather than duplicating transport contracts by hand where generation is practical.
