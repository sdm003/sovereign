# Infrastructure

This path is reserved for managed SaaS runtime and operational assets.

Expected contents in later issues:

- deployment configuration
- runtime environment contracts
- secret and migration conventions
- observability and runbook assets

This path should not contain application business logic.
